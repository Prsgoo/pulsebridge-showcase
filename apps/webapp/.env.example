# URL of the PulseBridge server the dashboard talks to.
# Unset in local dev — defaults to the served host on :3000.
# The Vercel demo build sets this to the public demo server.
VITE_API_URL=https://api-demo.prsgoo.com